local FormatDefinition = require("sdk.format.formatdefinition")
local TimFormat = require("formats.tim.definition")

FormatDefinition.register(TimFormat, "TIM Format", "Sony Playstation 1", "Dax", "1.0")