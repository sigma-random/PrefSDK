local FormatDefinition = require("sdk.format.formatdefinition")
local BitmapFormat = require("formats.bitmap.definition")

FormatDefinition.register(BitmapFormat, "Bitmap Format", "Imaging", "Dax", "1.1")